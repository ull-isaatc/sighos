/**
 * Provides the classes necessary to process the simulation state, that is, to store 
 * wherever it would be desired the state of a simulation.
 * The {@link es.ull.isaatc.simulation.state.processor.NullStateProcessor} can be used as a void processor
 * (nothing is done with the state). 
 */
package es.ull.isaatc.simulation.state.processor;
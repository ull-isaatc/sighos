/**
 * Contains the listener classes. These classes provides a mechanism to process
 * what is happening during the simulation through {@link es.ull.isaatc.simulation.info.SimulationInfo}
 * objects.<p>
 * All listeners implements {@link es.ull.isaatc.simulation.listener.EventListener}.<p>
 * The task of processing the information adquired by the listeners falls on the
 * {@link es.ull.isaatc.simulation.listener.ListenerController} class. 
 */
package es.ull.isaatc.simulation.listener;